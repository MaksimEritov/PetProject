export enum BotType {
    FOO_BOT = "FOO_BOT",
}

export enum BotStatus {
    IDLE = "IDLE",
    RUNNING = "RUNNING",
    PAUSED = "PAUSED",
    STOPPED = "STOPPED",
}

export enum RangeDirection {
    TOP = "TOP",
    BOTTOM = "BOTTOM",
}
